import 'package:flutter/material.dart';

class MainEpisodios extends StatelessWidget {
  const MainEpisodios({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Column(
          children: [Text("asas")],
        ),
      ),
    );
  }
}
